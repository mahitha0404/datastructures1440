#include<stdio.h>
int main()
{
	int n,i,a[20];
	printf("enter the size of the array :");
	scanf("%d",&n);
		printf("enter the values :\n");
		for(i=0;i<n;i++)
		{
			scanf("%d",&a[i]);
		}
			printf("the even numbera are \n");
			for(i=0;i<n;i++)
			{
				if(a[i]%2==0)
				printf("%d\t",a[i]);
				
			}
			printf("\n odd number in an array\n");
			for(i=0;i<n;i++)
			{
				if(a[i]%2!=0)
					printf("%d\t",a[i]);
			}
		return 0;
}




output

enter the size of the array :4
enter the values :
3 4 4 5 6
the even numbera are
4       4
 odd number in an array
3       5

